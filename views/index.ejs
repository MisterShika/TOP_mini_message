<html>
    <body>
        <ul>
            <% for(let i = 0; i < messages.length; i++) { %>
                <li>
                    <span class="name"><%= messages[i].text %></span>
                </li>
            <% } %>
        </ul>   
    </body>
</html>